---
# ============================================
# NTP Hardening for UNIX Variants
# AIX / Solaris / HP-UX
# ============================================

- name: Detect OS type
  ansible.builtin.raw: uname
  register: os_name
  changed_when: false

- name: Set NTP paths and restart command per OS
  ansible.builtin.set_fact:
    ntp_conf_path: >-
      {% if 'SunOS' in os_name.stdout %}
        /etc/inet/ntp.conf
      {% else %}
        /etc/ntp.conf
      {% endif %}
    ntp_restart_cmd: >-
      {% if 'AIX' in os_name.stdout %}
        stopsrc -s xntpd; startsrc -s xntpd
      {% elif 'SunOS' in os_name.stdout %}
        svcadm restart ntp
      {% elif 'HP-UX' in os_name.stdout %}
        /sbin/init.d/xntpd restart
      {% else %}
        echo "Unsupported UNIX OS"
      {% endif %}

# ------------------------------------------------
# Ensure NTP config file exists
# ------------------------------------------------
- name: Ensure NTP config file exists
  ansible.builtin.raw: |
    [ -f {{ ntp_conf_path }} ] || touch {{ ntp_conf_path }}
  changed_when: false

# ------------------------------------------------
# Backup existing config (only if non-empty)
# ------------------------------------------------
- name: Backup existing NTP config if present
  ansible.builtin.raw: |
    if [ -s {{ ntp_conf_path }} ]; then
      cp {{ ntp_conf_path }} {{ ntp_conf_path }}.bak_{{ timestamp }}
    fi
  changed_when: false

# ------------------------------------------------
# Configure NTP servers (managed block)
# ------------------------------------------------
- name: Configure NTP servers (ANSIBLE MANAGED)
  ansible.builtin.raw: |
    awk '
    BEGIN {found=0}
    /^# BEGIN ANSIBLE MANAGED NTP SERVERS/ {found=1}
    /^# END ANSIBLE MANAGED NTP SERVERS/ {found=0; next}
    {if (!found) print}
    END {
      print "# BEGIN ANSIBLE MANAGED NTP SERVERS"
    {% for server in ntp_servers %}
      print "server {{ server }} iburst"
    {% endfor %}
      print "# END ANSIBLE MANAGED NTP SERVERS"
    }' {{ ntp_conf_path }} > /tmp/ntp.conf && mv /tmp/ntp.conf {{ ntp_conf_path }}
  notify: Restart NTP service